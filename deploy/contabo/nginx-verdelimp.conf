# Nginx reverse proxy para Verdelimp ERP em VPS Contabo (compartilhada)
# Este arquivo é um site ADICIONAL — não altera os sites existentes (EJC, S2, escritório).
# Substitua erp.seudominio.com.br pelo domínio real e confira a porta (APP_PORT).

server {
    listen 80;
    server_name erp.seudominio.com.br;

    # Uploads de XML de NF-e e documentos do GED
    client_max_body_size 25m;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        proxy_pass http://127.0.0.1:3010;  # deve bater com APP_PORT do .env.production
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Depois de emitir SSL com Certbot, o próprio Certbot pode criar o bloco HTTPS.
# Comando sugerido:
# certbot --nginx -d erp.seudominio.com.br
