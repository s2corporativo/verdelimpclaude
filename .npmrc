# next-auth v4 declara peerOptional nodemailer ^7, mas o 9.x corrige
# vulnerabilidade alta (SMTP injection) e o provider de e-mail do next-auth
# não é usado (login por credenciais). Sem isto, npm ci falha com ERESOLVE
# no CI, na Vercel e no build Docker.
legacy-peer-deps=true
